# Copy this file to .env and fill in values as needed.
# All tools are optional — the devcontainer works without any of them.

# ── AI Coding Tools (set to true to install) ──────────────────────
ENABLE_CLAUDE_CODE=false
ENABLE_OPENCODE=false
ENABLE_CODEX=false

# ── Git and SSH (required for git operations) ─────────────────────
# These default to ~/.ssh and ~/.gitconfig if not set
SSH_PATH=~/.ssh
GITCONFIG_PATH=~/.gitconfig

# ── Zsh config (optional - set to your .zshrc path) ───────────────
# Leave unset or use /dev/null to skip
# ZSHRC_PATH=~/.zshrc

# ── Claude Code settings (mounted from host - persists across rebuilds) ──
# Note: OAuth credentials stored in Docker volume (lost on rebuilds)
# CLAUDE_SETTINGS_PATH=~/.claude/settings.json
# CLAUDE_MCP_PATH=~/.claude/mcp.json

# ── OpenCode data paths (optional - for persisting config) ────────
# OPENCODE_CONFIG_PATH=~/.config/opencode
# OPENCODE_DATA_PATH=~/.local/share/opencode

# ── Codex data path (optional - for persisting config) ────────────
# CODEX_PATH=~/.codex
